{
  "name": "union-backend",
  "version": "1.0.0",
  "description": "Real cloud backend for the UNION member portal — replaces client-side localStorage/public-JSON-bin sync with a proper server and authentication.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2"
  },
  "engines": {
    "node": ">=18"
  }
}
